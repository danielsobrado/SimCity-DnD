{
  "name": "simcity-dnd",
  "private": true,
  "version": "0.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:assets": "python scripts/generate-placeholder-models.py",
    "validate:assets": "node scripts/validate-assets.mjs",
    "test": "node --test",
    "verify": "npm run validate:assets && npm test && npm run build"
  },
  "dependencies": {
    "js-yaml": "4.1.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
