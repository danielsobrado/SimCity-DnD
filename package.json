{
  "name": "simcity-dnd",
  "private": true,
  "version": "0.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:assets": "python scripts/generate-placeholder-models.py",
    "bake:impostors": "node scripts/bake-tree-impostors.mjs",
    "validate:assets": "node scripts/validate-assets.mjs && node scripts/validate-impostors.mjs",
    "test": "node --test",
    "qa:perf": "node scripts/run-perf-qa.mjs",
    "qa:perf:parse": "node scripts/parse-perf-qa.mjs tmp/perf-qa-latest.json",
    "verify": "npm run validate:assets && npm test && npm run build"
  },
  "dependencies": {
    "js-yaml": "4.1.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
