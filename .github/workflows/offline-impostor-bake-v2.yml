name: Cancel superseded offline impostor bake v2

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/offline-impostor-bake-v2.yml

concurrency:
  group: offline-impostor-bake-v2
  cancel-in-progress: true

jobs:
  cancel-legacy:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo cancelled
