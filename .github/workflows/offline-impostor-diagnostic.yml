name: Offline impostor trigger

# Synchronization marker for the base-branch production bake.
on:
  workflow_dispatch:

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The base-branch bake workflow handles pull-request diagnostics."
